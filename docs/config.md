# Configuration

Status: incomplete stub for Milestone 0.

This document will describe the reviewbot TOML config, defaults, validation
rules, and examples. Current behavior is defined in `packages/core/src/config.ts`
and `SPEC.md`.
